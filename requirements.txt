torch>=2.2
tokenizers>=0.15
numpy>=1.24
pyarrow>=15          # reading corpus parquet shards in data/fetch.py

# added as each stage lands:
# sentence-transformers   # semantic preservation (FR-06 / SYS-F13)
# textstat                # readability metrics (SYS-F12)
# streamlit               # local app (SRD 9)
