.venv/
__pycache__/
*.pyc

# large, regenerable artefacts -- keep the pipeline in git, not its output
data/raw/
data/processed/
data/splits/
training/checkpoints/
tokenizer/tokenizer_files/
experiments/*.csv

.DS_Store

.claude/settings.local.json
data/.fetch_state.json
data/*.parquet
data/.*.parquet
data/tokenizer_sample.txt

# data/SOURCES.md is deliberately NOT ignored -- it is the licence record PRD 11 asks for

# third-party dataset: redistribution rights unverified, keep it local
data/human_ai/
evaluation/figures/

# live training log: appends for ~48h, regenerated per run
train.log
